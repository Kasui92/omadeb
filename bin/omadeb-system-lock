#!/bin/bash

# omadeb:summary=Lock the screen.
# omadeb:group=system
# omadeb:name=lock
# omadeb:examples=omadeb system lock

# Ensure 1password is locked
if pgrep -x "1password" >/dev/null; then
  1password --lock &
fi

# Lock the screen
loginctl lock-session