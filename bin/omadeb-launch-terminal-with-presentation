#!/bin/bash

# omadeb:summary=Launch a floating terminal with the Omadeb presentation wrapper.
# omadeb:args=<command>

cmd="$*"
xdg-terminal-exec bash -c "omadeb-show-logo; $cmd; if [ \$? -ne 130 ]; then omadeb-show-done; fi"
