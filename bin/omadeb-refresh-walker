#!/bin/bash

# omadeb:summary=Overwrite the user configs for Walker (app launcher and Omadeb Menu) and restart the services.

mkdir -p ~/.config/autostart/
cp $OMADEB_PATH/default/walker/walker.desktop ~/.config/autostart/

# And restarts if it crashes or is killed
mkdir -p ~/.config/systemd/user/app-walker@autostart.service.d/
cp $OMADEB_PATH/default/walker/restart.conf ~/.config/systemd/user/app-walker@autostart.service.d/restart.conf

systemctl --user daemon-reload

# Refresh configs
omadeb-refresh-config walker/config.toml
omadeb-refresh-config elephant/calc.toml
omadeb-refresh-config elephant/desktopapplications.toml

# Link all elephant menus
mkdir -p ~/.config/elephant/menus
for menu in $OMADEB_PATH/default/elephant/*.lua; do
  ln -snf "$menu" ~/.config/elephant/menus/"$(basename "$menu")"
done

# Restart service
omadeb-restart-walker
