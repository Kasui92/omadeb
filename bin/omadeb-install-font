#!/bin/bash

# omadeb:summary=Install a font for use in the terminal and code editor.
# omadeb:args=<font>
# omadeb:examples=omadeb-install-font jetbrains-mono

if [[ -z $1 ]]; then
  echo "Usage: omadeb-install-font <font>" >&2
  exit 1
fi

mkdir -p ~/.local/share/fonts

case "$1" in
jetbrains-mono)
  omadeb-pkg-add fonts-jetbrains-mono
  ;;
iafonts)
  omadeb-pkg-add fonts-ia-writer-mono
  ;;
cascadia-mono)
  omadeb-pkg-add fonts-cascadia-mono-nf
  ;;
mscore)
  omadeb-pkg-add ttf-mscorefonts-installer
  ;;
*)
  echo "Usage: omadeb-install-font <font>" >&2
  exit 1
  ;;
esac

fc-cache