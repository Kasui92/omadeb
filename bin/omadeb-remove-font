#!/bin/bash

# omadeb:summary=Remove one of the supported fonts.
# omadeb:args=<jetbrains-mono|iafonts|cascadia-mono|mscore>

if [[ -z "$1" ]]; then
  echo "Usage: omadeb-remove-font <font>" >&2
  exit 1
fi


case "$1" in
jetbrains-mono)
  omadeb-pkg-remove fonts-jetbrains-mono
  ;;
iafonts)
  omadeb-pkg-remove fonts-ia-writer-mono
  ;;
cascadia-mono)
  omadeb-pkg-remove fonts-cascadia-mono-nf
  ;;
mscore)
  omadeb-pkg-remove ttf-mscorefonts-installer
  ;;
*)
  echo "Usage: omadeb-remove-font <font>" >&2
  exit 1
  ;;
esac

fc-cache