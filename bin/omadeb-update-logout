#!/bin/bash

# omadeb:summary=Prompt for logout if updates require it.

if [[ -f $HOME/.local/state/omadeb/logout-required ]]; then
  echo
  gum confirm "Updates require logout. Ready?" && omadeb-system-logout
fi
