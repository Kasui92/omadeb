#!/bin/bash

# omadeb:summary=Log out the current user session.
# omadeb:examples=omadeb system logout

omadeb-state clear logout-required
sleep 1 # Allow apps like Chrome to shutdown correctly
gnome-session-quit --logout --no-prompt
