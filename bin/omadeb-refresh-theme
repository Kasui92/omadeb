#!/bin/bash

# omadeb:summary=Reapply the current Omadeb theme.

THEME_NAME_PATH="$HOME/.config/omadeb/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  OMADEB_THEME_SKIP_BACKGROUND=1 omadeb-theme-set "$(cat $THEME_NAME_PATH)"
fi