#!/bin/bash

# omadeb:summary=Pull the latest Omadeb git changes.

set -e

echo -e "\e[32mUpdate Omadeb\e[0m"

git -C $OMADEB_PATH pull --autostash
git -C $OMADEB_PATH --no-pager diff --check || git -C $OMADEB_PATH reset --merge